import json
import itertools

def parse(json-file-name):
  '''this function takes a json file as an input and returns a list of letters that are recognized with at least confidence level of at least 0.1'''
  return letters
